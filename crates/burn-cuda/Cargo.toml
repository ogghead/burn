[package]
authors = ["nathanielsimard <nathaniel.simard.42@gmail.com>"]
categories = ["science"]
description = "CUDA backend for the Burn framework"
documentation = "https://docs.rs/burn-cuda"
edition.workspace = true
keywords = ["deep-learning", "machine-learning", "gpu", "cuda"]
license.workspace = true
name = "burn-cuda"
readme.workspace = true
repository = "https://github.com/tracel-ai/burn/tree/main/crates/burn-cuda"
version.workspace = true

[lints]
workspace = true

[features]
# NOTE (diffusion-app): `fusion` is back in the default (fused inference). It was
# briefly removed to isolate the high-res LoRA-training OOM, but block-level
# gradient checkpointing (flux-klein lora_train/checkpoint.rs) — which runs each
# block detached/no-grad in the forward and recomputes in the backward — bounds
# the live activation footprint independently of fusion's lazy buffering, so
# fusion can stay on for both inference and training.
default = ["std", "autotune", "fusion", "burn-cubecl/default", "cubecl/default"]
doc = ["burn-cubecl/doc"]
std = ["burn-cubecl/std", "cubecl/std"]
tracing = [
    "burn-backend/tracing",
    "burn-cubecl/tracing",
    "burn-fusion?/tracing",
    "cubecl/tracing",
]

fusion = ["burn-fusion", "burn-cubecl/fusion"]
autotune = ["burn-cubecl/autotune"]
autotune-checks = ["burn-cubecl/autotune-checks"]
distributed = ["std", "burn-cubecl/distributed", "burn-fusion?/distributed"]

[dependencies]
burn-fusion = { workspace = true, optional = true, features = ["default"] }
burn-cubecl = { workspace = true }
burn-backend = { workspace = true, features = [
    "cubecl-cuda",
] }
cubecl = { workspace = true, features = ["cuda"] }

[package.metadata.docs.rs]
features = ["doc"]
rustdoc-args = ["--cfg", "docsrs"]
